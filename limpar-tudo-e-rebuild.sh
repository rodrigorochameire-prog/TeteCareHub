<<<<<<< Current (Your changes)
#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"


#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"



#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"


#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"



#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"


#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"



#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"


#!/bin/bash

echo "🧹 Limpando tudo..."

# Parar processos
echo "1. Parando processos..."
pkill -f "vite|node.*dev|tsx" 2>/dev/null || true
sleep 2

# Limpar caches
echo "2. Limpando caches..."
rm -rf dist
rm -rf node_modules/.vite
rm -rf .vite
rm -rf client/dist
rm -rf server/dist

# Rebuild
echo "3. Rebuild completo..."
npm run build

echo "✅ Pronto! Agora:"
echo "   1. Limpe o cache do navegador (Ctrl+Shift+Delete)"
echo "   2. Feche todas as abas do localhost:3000"
echo "   3. Inicie o servidor: npm run dev"
echo "   4. Abra o navegador em modo anônimo ou recarregue a página"





=======
>>>>>>> Incoming (Background Agent changes)
