Por favor, execute a **Opção 1**: DROP das funções existentes e recriação.

**Justificativa:**
- As funções foram criadas recentemente durante este processo de configuração
- Não há dependências externas além das políticas RLS que estamos criando agora
- É mais simples e limpo recriar tudo de uma vez
- O script já foi atualizado para fazer DROP antes de criar

**Ações a executar:**
1. DROP FUNCTION public.is_tutor_of_pet(uuid, int) CASCADE;
2. DROP FUNCTION public.extract_pet_id_from_path(text) CASCADE;
3. DROP FUNCTION public.is_admin(uuid) CASCADE;
4. DROP FUNCTION public.extract_tutor_id_from_path(text) CASCADE;
5. Recriar todas as funções com os tipos corretos
6. Aplicar todas as políticas RLS

**Confirmação:** Pode prosseguir com o DROP e recriação das funções, seguido da aplicação das políticas.


