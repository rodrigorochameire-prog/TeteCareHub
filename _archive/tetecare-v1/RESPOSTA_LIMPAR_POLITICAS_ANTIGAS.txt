Sim — listar. Por favor, execute os seguintes passos:

1. **Listar todas as políticas** em storage.objects
2. **Identificar políticas não conformes** que não seguem o padrão:
   - Padrão correto: `{bucket}_policy_{operacao}` onde operacao = select, insert, update ou delete
   - Exemplos corretos: `pets_policy_select`, `documents_policy_insert`, `wall_policy_update`
   - Exemplos incorretos: `storage_documents_user_read`, `documents_auth_select`, etc.
3. **Retornar a lista** de políticas não conformes para minha revisão
4. **Após minha confirmação**, executar DROP POLICY para remover apenas as políticas antigas/duplicadas

**Critério de identificação:**
- Políticas que NÃO seguem o padrão `{bucket}_policy_{select|insert|update|delete}`
- Mas que são relacionadas aos nossos buckets (pets, documents, daycare-photos, etc.)

**Políticas que devem ser MANTIDAS:**
- Qualquer política que siga o padrão `{bucket}_policy_{select|insert|update|delete}`
- Exemplos: `pets_policy_select`, `documents_policy_insert`, `wall_policy_update`, etc.

Pode prosseguir com a listagem e identificação das políticas não conformes.


