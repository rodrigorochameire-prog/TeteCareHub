Sim, insira com owner NULL.

Autorizado a prosseguir com:
1. Criar bucket "pets" com owner = NULL e public = true (se não existir)
2. Criar bucket "documents" com owner = NULL e public = false (se não existir)
3. Validar que os buckets foram criados
4. Criar as políticas RLS conforme planejado

Se a inserção com owner = NULL falhar (porque a coluna não permite NULL), por favor:
- Tente primeiro verificar a estrutura da tabela storage.buckets
- Se necessário, use um UUID válido (pode ser o UUID do usuário service_role ou admin)
- Ou me informe o erro específico para eu decidir a próxima ação

Para as políticas RLS:
- Se storage.foldername(name) não estiver disponível, use apenas a verificação de bucket_id
- Para "pets": permita SELECT público e INSERT para autenticados
- Para "documents": permita SELECT e INSERT apenas para autenticados (pode simplificar sem verificação de pasta se necessário)

Pode prosseguir.


