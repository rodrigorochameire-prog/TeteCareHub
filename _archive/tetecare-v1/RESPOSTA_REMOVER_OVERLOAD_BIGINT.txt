Por favor, execute a opção "drop bigint": remover a sobrecarga `(uuid, bigint)` e manter apenas a versão `(uuid, integer)`.

**Justificativa:**
- O schema usa `integer` para `petId` e `tutorId` na tabela `public.pet_tutors`
- A versão `(uuid, integer)` que criamos é a correta e usa as colunas `"petId"` e `"tutorId"` com os nomes corretos
- A versão `(uuid, bigint)` parece ser uma versão antiga ou criada anteriormente com tipos/nomes incorretos

**Ação a executar:**
```sql
DROP FUNCTION public.is_tutor_of_pet(uuid, bigint);
```

**Após executar, valide:**
```sql
SELECT proname, pg_get_function_arguments(oid) as arguments
FROM pg_proc 
WHERE pronamespace = 'public'::regnamespace 
  AND proname = 'is_tutor_of_pet';
```

**Resultado esperado:** Apenas uma função `is_tutor_of_pet(uuid, integer)` deve aparecer.

Pode prosseguir com a remoção da sobrecarga `(uuid, bigint)`.


