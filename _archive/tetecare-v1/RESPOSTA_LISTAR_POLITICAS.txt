OK. Por favor, execute a consulta para listar todas as políticas com suas definições completas usando `pg_get_policydef()`.

Após listar, identifique e remova as políticas antigas/duplicadas que não seguem o padrão `{bucket}_policy_{operacao}`.

Políticas que devem ser mantidas (padrão correto):
- `{bucket}_policy_select`
- `{bucket}_policy_insert`
- `{bucket}_policy_update`
- `{bucket}_policy_delete`

Políticas que devem ser removidas (antigas/duplicadas):
- Qualquer política que não siga o padrão acima
- Exemplos: `storage_documents_user_read`, `documents_auth_select`, etc.

Pode prosseguir com a consulta e depois com a limpeza das políticas antigas.


